diff --git a/backend/coreapp/compilers.py b/backend/coreapp/compilers.py
index 04adb16..6d16042 100644
--- a/backend/coreapp/compilers.py
+++ b/backend/coreapp/compilers.py
@@ -134,6 +134,13 @@ class SHCOldCompiler(Compiler):
     library_include_flag: str = ""
 
 
+@dataclass(frozen=True)
+class SHCSaturnCompiler(Compiler):
+    platform: Platform = SATURN
+    flag_class: ClassVar[flags.FlagClass] = flags.SHC_SH2_FLAGS
+    library_include_flag: str = ""
+
+
 @dataclass(frozen=True)
 class GCCCompiler(Compiler):
     type: ClassVar[CompilerType] = CompilerType.GCC
@@ -619,6 +626,17 @@ SHC_V51R08 = SHCCompiler(id="shc-v5.1r08", platform=DREAMCAST, cc=DREAMCAST_CC)
 SHC_V51R11 = SHCCompiler(id="shc-v5.1r11", platform=DREAMCAST, cc=DREAMCAST_CC)
 SHC_V51R13 = SHCCompiler(id="shc-v5.1r13", platform=DREAMCAST, cc=DREAMCAST_CC)
 
+SATURN_SHC_CC = (
+    'cat "$INPUT" | unix2dos > dos_src.c && '
+    "cp -r ${COMPILER_DIR}/bin/* . && "
+    "(SHC_LIB=. SHC_TMP=. ${WIBO} ${COMPILER_DIR}/bin/shc.exe dos_src.c -comment=nonest -cpu=sh2 -endian=big -sjis -string=const ${COMPILER_FLAGS} -object=dos_src.obj) && "
+    "python3 ${COMPILER_DIR}/rof2elf.py dos_src.obj ${OUTPUT} --isa=sh2"
+)
+
+SHC_V50R32_SH2 = SHCSaturnCompiler(
+    id="shc-v5.0r32-sh2", cc=SATURN_SHC_CC, base_compiler=SHC_V50R32
+)
+
 # PS2
 IOP_GCC281 = GCCPS2Compiler(
     id="iop-gcc2.8.1",
@@ -1797,6 +1815,7 @@ _all_compilers: list[Compiler] = [
     MWCCPSP_3_0_1_219,
     # Saturn
     CYGNUS_2_7_96Q3,
+    SHC_V50R32_SH2,
     # Dreamcast
     SHC_V50R10,
     SHC_V50R26,
diff --git a/backend/coreapp/flags.py b/backend/coreapp/flags.py
index 519ce37..8f38090 100644
--- a/backend/coreapp/flags.py
+++ b/backend/coreapp/flags.py
@@ -238,6 +238,20 @@ SHC_FLAGS = FlagClass(
     ],
     parent=COMMON_SHC_OLD_FLAGS,
 )
+
+SHC_SH2_FLAGS = FlagClass(
+    name="shc-sh2",
+    flags=[
+        FlagSet(
+            id="shc_division",
+            flags=["-division=cpu", "-division=peripheral", "-division=nomask"],
+        ),
+        FlagSet(id="shc_macsave", flags=["-macsave=0", "-macsave=1"]),
+        Checkbox(id="shc_aggressive", flag="-aggressive=2"),
+    ],
+    parent=COMMON_SHC_OLD_FLAGS,
+)
+
 COMMON_GCC_FLAGS = FlagClass(
     name="gcc",
     flags=[
diff --git a/frontend/src/lib/i18n/locales/en/compilers.json b/frontend/src/lib/i18n/locales/en/compilers.json
index 5ff751d..b9df346 100644
--- a/frontend/src/lib/i18n/locales/en/compilers.json
+++ b/frontend/src/lib/i18n/locales/en/compilers.json
@@ -269,6 +269,7 @@
     "msc5.1": "Microsoft C Compiler 5.1",
 
     "cygnus-2.7-96Q3": "cygnus-2.7-96Q3 SOA-960904",
+    "shc-v5.0r32-sh2": "SHC v5.0 (Release 32)",
 
     "ndk-r8e-gcc-4.4.3": "Android NDK r8e (GCC 4.4.3)",
     "ndk-r8e-gcc-4.7": "Android NDK r8e (GCC 4.7)",
@@ -507,6 +508,13 @@
     "shc_aggressive": "Use float multiplication for division with constant",
     "shc_loop": "Loop unrolling",
     "shc_inline": "Inline static functions",
+    "shc_division": "Division code expansion",
+    "shc_division.-division=cpu": "Division: CPU",
+    "shc_division.-division=peripheral": "Division: on-chip divider, interrupts masked",
+    "shc_division.-division=nomask": "Division: on-chip divider, interrupts not masked",
+    "shc_macsave": "MACL/MACH save rules",
+    "shc_macsave.-macsave=0": "Don't save MACL/MACH",
+    "shc_macsave.-macsave=1": "Save MACL/MACH (callee-saved)",
 
     "psyq_opt_level": "Optimization level",
     "psyq_opt_level.-O0": "No optimization",
